import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: "/synapse-cs", // 🚀 Semua fungsi router.push("/dashboard") otomatis menjadi /synapse-cs/dashboard
};

export default nextConfig;